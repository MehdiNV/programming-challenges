def rotLeft(a, d):
    resultArray=[]
    loopCounter=d

    

    for i in a:
        if (i==a[0]):
            resultArray.append(a[loopCounter])
            continue
        
        loopCounter+=1
        resultArray.append(a[loopCounter-(len(a))])
    

    return (resultArray)
